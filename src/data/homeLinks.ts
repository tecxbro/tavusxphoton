/** Fixed CTA destination after hangup / exit from a call. */
export const HIRE_ME_URL = "https://pleasegivemeaninternship.com";

/**
 * Only approved external destination in this app.
 * Do not invent profile URLs.
 */
export const APPROVED_EXTERNAL_URL = HIRE_ME_URL;
