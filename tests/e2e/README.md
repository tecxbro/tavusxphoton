# E2E specs parked

Browser e2e specs for this PRD live in:

`docs/chromium-e2e-review/`

They are disabled here on purpose so `npm run check` / local work focuses on the app build. Helpers remain in `helpers/` for when you move the specs back.
