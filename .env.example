# Server only — never use a VITE_ prefix for these
TAVUS_API_KEY=
TAVUS_PAL_ID=
TAVUS_FACE_ID=

# Optional. Prefer shell override when starting for a real call:
#   TAVUS_TEST_MODE=false npm run dev
# When unset, vite.config.ts defaults to true (PAL does not join).
# TAVUS_TEST_MODE=true
